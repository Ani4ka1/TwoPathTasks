<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TwoPathTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Настройка цветов для статус бара -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Другие стили для вашего приложения -->
    </style>

    <!-- Тема для Activity, если нужно -->
    <style name="AppTheme.Activity" parent="AppTheme">
        <!-- Настройка цветов для статус бара -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Другие стили для Activity -->
    </style>

    <style name="Theme.TwoPathTask" parent="Base.Theme.TwoPathTask" />
</resources>